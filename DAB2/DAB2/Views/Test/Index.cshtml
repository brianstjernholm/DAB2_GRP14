@model IEnumerable<DAB2.Models.StudentAssignmentModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student.AuId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assignment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.AuId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Assignment.AssignmentId)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.StudentAssignmentModelId }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */}) |
                @Html.ActionLink("Delete", "Delete", new { id = item.StudentAssignmentModelId })
            </td>
        </tr>
}
    </tbody>
</table>
